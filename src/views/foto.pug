- var activePage = 'foto';
<!DOCTYPE html>
html(lang="pl")
head
 include includes/meta
body(class=(activePage === 'foto' ? "body-gallery": ""))
  include includes/analytics
  include includes/header
  h2(class='photo-h2')
  include includes/gallery
  include includes/footer
